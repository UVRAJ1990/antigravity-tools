"""
submit_urls.py — Google Indexing API URL Submitter
Submits 10 un-indexed URLs per day from sitemap.xml
Works both locally and in GitHub Actions
"""

import json, time, base64, urllib.request, urllib.parse, urllib.error, re, os, rsa
from datetime import datetime, timezone

# ── Config ────────────────────────────────────────────────────────────────────
CREDS_FILE   = "sc_credentials.json"
SITEMAP_FILE = "sitemap.xml"
LOG_FILE     = "submission_log.json"
BATCH_SIZE   = 10
DELAY_SECS   = 1.0

# ── Load credentials (from GitHub Secret or local file) ────────────────────────
sc_env = os.environ.get("SC_CREDENTIALS")
if sc_env:
    creds = json.loads(sc_env)
    print("Credentials loaded from environment variable.")
else:
    with open(CREDS_FILE) as f:
        creds = json.load(f)
    print("Credentials loaded from file.")

# ── JWT / Token helpers ───────────────────────────────────────────────────────
def b64url(data: bytes) -> str:
    return base64.urlsafe_b64encode(data).rstrip(b"=").decode()

def make_jwt(sa: dict, scope: str) -> str:
    now = int(time.time())
    header  = b64url(json.dumps({"alg":"RS256","typ":"JWT"}).encode())
    payload = b64url(json.dumps({
        "iss": sa["client_email"],
        "scope": scope,
        "aud": sa["token_uri"],
        "iat": now, "exp": now + 3600,
    }).encode())
    signing_input = f"{header}.{payload}".encode()
    pem_body = sa["private_key"]
    der = base64.b64decode("".join(pem_body.strip().splitlines()[1:-1]))
    idx = der.find(b'\x30\x82', 20)
    privkey = rsa.PrivateKey._load_pkcs1_der(der[idx:])
    sig = rsa.sign(signing_input, privkey, "SHA-256")
    return f"{header}.{payload}.{b64url(sig)}"

def get_token(scope: str) -> str:
    jwt = make_jwt(creds, scope)
    data = urllib.parse.urlencode({
        "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
        "assertion": jwt,
    }).encode()
    req = urllib.request.Request(creds["token_uri"], data=data,
        headers={"Content-Type": "application/x-www-form-urlencoded"})
    with urllib.request.urlopen(req) as r:
        return json.load(r)["access_token"]

# ── Submission log helpers ────────────────────────────────────────────────────
def load_log() -> dict:
    try:
        with open(LOG_FILE, encoding="utf-8") as f:
            content = f.read().strip()
            if not content:
                return {"submitted": {}, "runs": []}
            return json.loads(content)
    except (FileNotFoundError, json.JSONDecodeError):
        return {"submitted": {}, "runs": []}

def save_log(log: dict):
    tmp = LOG_FILE + ".tmp"
    with open(tmp, "w", encoding="utf-8") as f:
        json.dump(log, f, indent=2, ensure_ascii=False)
    os.replace(tmp, LOG_FILE)

# ── Read sitemap URLs ─────────────────────────────────────────────────────────
def get_sitemap_urls() -> list:
    with open(SITEMAP_FILE, encoding="utf-8") as f:
        content = f.read()
    return re.findall(r"<loc>(https://antigravitytools\.app[^<]*)</loc>", content)

# ── Submit URL via Indexing API ───────────────────────────────────────────────
def submit_url(token: str, url: str) -> dict:
    body = json.dumps({"url": url, "type": "URL_UPDATED"}).encode()
    req = urllib.request.Request(
        "https://indexing.googleapis.com/v3/urlNotifications:publish",
        data=body,
        headers={"Authorization": f"Bearer {token}", "Content-Type": "application/json"},
        method="POST"
    )
    try:
        with urllib.request.urlopen(req, timeout=15) as r:
            return {"status": "ok", "response": json.load(r)}
    except urllib.error.HTTPError as e:
        return {"status": "error", "code": e.code, "error": e.read().decode()}
    except Exception as e:
        return {"status": "error", "error": str(e)}

# ── Main ──────────────────────────────────────────────────────────────────────
now_str = datetime.now(timezone.utc).strftime("%Y-%m-%d %H:%M:%S UTC")
print("=" * 65)
print("  Antigravity Tools - URL Submission Run")
print(f"  {now_str}")
print("=" * 65)

print("\nAuthenticating...")
token = get_token("https://www.googleapis.com/auth/indexing")
print("Token OK\n")

log            = load_log()
submitted_urls = set(log["submitted"].keys())
all_urls       = get_sitemap_urls()
pending        = [u for u in all_urls if u not in submitted_urls]

print(f"Total URLs in sitemap : {len(all_urls)}")
print(f"Already submitted     : {len(submitted_urls)}")
print(f"Pending (never sent)  : {len(pending)}")

if not pending:
    sorted_done = sorted(log["submitted"].items(), key=lambda x: x[1].get("submitted_at",""))
    batch = [u for u, _ in sorted_done[:BATCH_SIZE]]
    print(f"\nAll done! Re-submitting {len(batch)} oldest to keep fresh.\n")
else:
    batch = pending[:BATCH_SIZE]
    print(f"This batch            : {len(batch)} URLs\n")

print("-" * 65)
results = {"ok": [], "error": []}

for i, url in enumerate(batch, 1):
    result = submit_url(token, url)
    ts = datetime.now(timezone.utc).strftime("%Y-%m-%d %H:%M:%S UTC")
    if result["status"] == "ok":
        print(f"  [{i:02d}] SUBMITTED  {url}")
        log["submitted"][url] = {"submitted_at": ts, "last_status": "ok"}
        results["ok"].append(url)
    else:
        code = result.get("code","?")
        err  = result.get("error","")[:100]
        print(f"  [{i:02d}] ERROR {code}  {url}")
        print(f"         {err}")
        log["submitted"][url] = {"submitted_at": ts, "last_status": f"error_{code}"}
        results["error"].append(url)
    time.sleep(DELAY_SECS)

log["runs"].append({
    "run_at": now_str, "submitted": len(results["ok"]),
    "errors": len(results["error"]), "urls": batch,
})
save_log(log)

print("\n" + "=" * 65)
print("  SUMMARY")
print("=" * 65)
print(f"  Submitted OK   : {len(results['ok'])}")
print(f"  Errors         : {len(results['error'])}")
print(f"  Remaining      : {max(0, len(pending) - len(results['ok']))}")
days_left = max(0, len(pending) - len(results['ok'])) // BATCH_SIZE
print(f"  Est. days left : ~{days_left} day(s)")
print(f"  Log saved to   : {LOG_FILE}")
print("=" * 65)"""
submit_urls.py — Google Indexing API URL Submitter
Submits 10 un-indexed URLs per day from sitemap.xml
Works both locally and in GitHub Actions
"""

import json, time, base64, urllib.request, urllib.parse, urllib.error, re, os, rsa
from datetime import datetime, timezone

# ── Config ────────────────────────────────────────────────────────────────────
CREDS_FILE   = "sc_credentials.json"
SITEMAP_FILE = "sitemap.xml"
LOG_FILE     = "submission_log.json"
BATCH_SIZE   = 10
DELAY_SECS   = 1.0

# ── Load credentials (from GitHub Secret or local file) ────────────────────────
sc_env = os.environ.get("SC_CREDENTIALS")
if sc_env:
    creds = json.loads(sc_env)
    print("Credentials loaded from environment variable.")
else:
    with open(CREDS_FILE) as f:
        creds = json.load(f)
    print("Credentials loaded from file.")

# ── JWT / Token helpers ───────────────────────────────────────────────────────
def b64url(data: bytes) -> str:
    return base64.urlsafe_b64encode(data).rstrip(b"=").decode()

def make_jwt(sa: dict, scope: str) -> str:
    now = int(time.time())
    header  = b64url(json.dumps({"alg":"RS256","typ":"JWT"}).encode())
    payload = b64url(json.dumps({
        "iss": sa["client_email"],
        "scope": scope,
        "aud": sa["token_uri"],
        "iat": now, "exp": now + 3600,
    }).encode())
    signing_input = f"{header}.{payload}".encode()
    pem_body = sa["private_key"]
    der = base64.b64decode("".join(pem_body.strip().splitlines()[1:-1]))
    idx = der.find(b'\x30\x82', 20)
    privkey = rsa.PrivateKey._load_pkcs1_der(der[idx:])
    sig = rsa.sign(signing_input, privkey, "SHA-256")
    return f"{header}.{payload}.{b64url(sig)}"

def get_token(scope: str) -> str:
    jwt = make_jwt(creds, scope)
    data = urllib.parse.urlencode({
        "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
        "assertion": jwt,
    }).encode()
    req = urllib.request.Request(creds["token_uri"], data=data,
        headers={"Content-Type": "application/x-www-form-urlencoded"})
    with urllib.request.urlopen(req) as r:
        return json.load(r)["access_token"]

# ── Submission log helpers ────────────────────────────────────────────────────
def load_log() -> dict:
    try:
        with open(LOG_FILE, encoding="utf-8") as f:
            content = f.read().strip()
            if not content:
                return {"submitted": {}, "runs": []}
            return json.loads(content)
    except (FileNotFoundError, json.JSONDecodeError):
        return {"submitted": {}, "runs": []}

def save_log(log: dict):
    tmp = LOG_FILE + ".tmp"
    with open(tmp, "w", encoding="utf-8") as f:
        json.dump(log, f, indent=2, ensure_ascii=False)
    os.replace(tmp, LOG_FILE)

# ── Read sitemap URLs ─────────────────────────────────────────────────────────
def get_sitemap_urls() -> list:
    with open(SITEMAP_FILE, encoding="utf-8") as f:
        content = f.read()
    return re.findall(r"<loc>(https://antigravitytools\.app[^<]*)</loc>", content)

# ── Submit URL via Indexing API ───────────────────────────────────────────────
def submit_url(token: str, url: str) -> dict:
    body = json.dumps({"url": url, "type": "URL_UPDATED"}).encode()
    req = urllib.request.Request(
        "https://indexing.googleapis.com/v3/urlNotifications:publish",
        data=body,
        headers={"Authorization": f"Bearer {token}", "Content-Type": "application/json"},
        method="POST"
    )
    try:
        with urllib.request.urlopen(req, timeout=15) as r:
            return {"status": "ok", "response": json.load(r)}
    except urllib.error.HTTPError as e:
        return {"status": "error", "code": e.code, "error": e.read().decode()}
    except Exception as e:
        return {"status": "error", "error": str(e)}

# ── Main ──────────────────────────────────────────────────────────────────────
now_str = datetime.now(timezone.utc).strftime("%Y-%m-%d %H:%M:%S UTC")
print("=" * 65)
print("  Antigravity Tools - URL Submission Run")
print(f"  {now_str}")
print("=" * 65)

print("\nAuthenticating...")
token = get_token("https://www.googleapis.com/auth/indexing")
print("Token OK\n")

log            = load_log()
submitted_urls = set(log["submitted"].keys())
all_urls       = get_sitemap_urls()
pending        = [u for u in all_urls if u not in submitted_urls]

print(f"Total URLs in sitemap : {len(all_urls)}")
print(f"Already submitted     : {len(submitted_urls)}")
print(f"Pending (never sent)  : {len(pending)}")

if not pending:
    sorted_done = sorted(log["submitted"].items(), key=lambda x: x[1].get("submitted_at",""))
    batch = [u for u, _ in sorted_done[:BATCH_SIZE]]
    print(f"\nAll done! Re-submitting {len(batch)} oldest to keep fresh.\n")
else:
    batch = pending[:BATCH_SIZE]
    print(f"This batch            : {len(batch)} URLs\n")

print("-" * 65)
results = {"ok": [], "error": []}

for i, url in enumerate(batch, 1):
    result = submit_url(token, url)
    ts = datetime.now(timezone.utc).strftime("%Y-%m-%d %H:%M:%S UTC")
    if result["status"] == "ok":
        print(f"  [{i:02d}] SUBMITTED  {url}")
        log["submitted"][url] = {"submitted_at": ts, "last_status": "ok"}
        results["ok"].append(url)
    else:
        code = result.get("code","?")
        err  = result.get("error","")[:100]
        print(f"  [{i:02d}] ERROR {code}  {url}")
        print(f"         {err}")
        log["submitted"][url] = {"submitted_at": ts, "last_status": f"error_{code}"}
        results["error"].append(url)
    time.sleep(DELAY_SECS)

log["runs"].append({
    "run_at": now_str, "submitted": len(results["ok"]),
    "errors": len(results["error"]), "urls": batch,
})
save_log(log)

print("\n" + "=" * 65)
print("  SUMMARY")
print("=" * 65)
print(f"  Submitted OK   : {len(results['ok'])}")
print(f"  Errors         : {len(results['error'])}")
print(f"  Remaining      : {max(0, len(pending) - len(results['ok']))}")
days_left = max(0, len(pending) - len(results['ok'])) // BATCH_SIZE
print(f"  Est. days left : ~{days_left} day(s)")
print(f"  Log saved to   : {LOG_FILE}")
print("=" * 65)
